Edit the existing project (DO NOT rebuild) to upgrade the **Map section** and **Dashboard background** into a realistic, production-quality experience.

---

## 1. Fix / Upgrade Existing Map Section (DO NOT CREATE NEW)

### Objective

Convert the current useless map into a **realistic, functional product navigation system**.

### Required Changes

#### A. Replace Fake Map with Real 3D Warehouse Model

* Remove any placeholder/flat/2D or primitive map
* Use a **real warehouse/supermarket GLB model**
* Load it using `useGLTF` inside the existing Map component

---

#### B. Connect Products to Real Locations

* Create a structured mapping:

```js
const productMap = {
  broccoli: { section: "groceries", position: [x, y, z] },
  biscuits: { section: "snacks", position: [x, y, z] }
};
```

* Use realistic coordinates matching positions inside the 3D model

---

#### C. Add Search → Navigation Logic

* When user searches a product:

  * Find product in `productMap`
  * Highlight that section in 3D
  * Move camera smoothly to that location
  * Display label: “Navigating to Groceries”

---

#### D. Visual Navigation (IMPORTANT)

* Draw a visible path:

  * Use a line or glowing path from user position → target
* Add marker or pointer at destination

---

#### E. Improve Realism

* Add:

  * `<Environment preset="warehouse" />`
  * Proper lighting (ambient + directional)
  * Shadows enabled
* Ensure correct scale and proportions

---

#### F. Interaction

* Allow limited camera movement (OrbitControls)
* Highlight sections on hover
* Smooth transitions (no sudden jumps)

---

## 2. Dashboard Background (REAL VIDEO, NOT FAKE)

### Objective

Add **real-life background videos** showing people walking with carts (Amazon Go style, no billing counters visible).

---

### Requirements

#### A. Use Real Video Footage

* Source from:

  * Pexels Videos
  * Pixabay Videos
* Keywords:

  * “shopping cart walking supermarket”
  * “people grocery shopping aisle”
  * “retail store walking customers”

---

#### B. Implementation

* Add **full-screen background video** in Dashboard:

```html
<video autoplay muted loop playsInline>
  <source src="/videos/store.mp4" type="video/mp4" />
</video>
```

---

#### C. Styling

* Position:

  * Fullscreen background
  * Behind dashboard UI
* Add overlay:

  * Dark gradient or blur for readability

---

#### D. Performance

* Use:

  * Compressed MP4 (not heavy files)
  * Autoplay + muted (required for browsers)
* Optionally lazy load video

---

## Constraints

* DO NOT rebuild entire app
* DO NOT replace login/dashboard logic
* ONLY enhance:

  * Existing Map section
  * Dashboard background
* MUST use real assets (no placeholders, no fake geometry)

---

## Expected Result

* Map becomes functional and realistic (not useless)
* User can search → see → navigate in 3D warehouse
* Dashboard feels premium with real-world retail video background
* Overall app looks professional, not demo-level

---

Generate only the modified Map component and Dashboard background integration code.
